{
  "buildCommand": "npm ci --legacy-peer-deps && npx prisma generate && npm run build",
  "devCommand": "npm run dev",
  "functions": {
    "app/api/**": {
      "maxDuration": 30,
      "memory": 1024
    }
  }
}
