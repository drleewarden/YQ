{
  "buildCommand": "npm ci --legacy-peer-deps && npx prisma generate --skip-engine-check && npm run build",
  "devCommand": "npm run dev",
  "functions": {
    "app/api/**": {
      "maxDuration": 30,
      "memory": 1024
    }
  }
}
